export const metadata = {
    title: "그룹| CRM",
    description: "그룹 관리 페이지입니다.",
};

const GroupsLayout = ({ children }: { children: React.ReactNode }) => {
    return <>{children}</>;
};

export default GroupsLayout;
